# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
# ...except the two apps' .env: they hold only the demo Supabase project's
# publishable anon key (client-safe by design) so a fresh clone runs with no setup.
!company-dashboard/.env
!student-app/.env

# editor / os
.vscode/
.idea/
.DS_Store
Thumbs.db

# logs
npm-debug.log*
yarn-error.log*
*.log

# historical design references from the discarded Figma Make prototype.
# Current designs live at the two figma.site links in CLAUDE.md.
uploads/
