node_modules/
dist/
build/
.cache/
.env.local
.env.*.local
!.env.example
# .env is committed: it holds only the demo project's client-safe anon key,
# so `npm run dev` works straight after a clone. Real deployments override it.
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
